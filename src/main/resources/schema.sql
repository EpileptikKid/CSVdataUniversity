DROP TABLE IF EXISTS Data_University;

CREATE TABLE Data_University (
    State VARCHAR(70) NOT NULL,
    NameUniversity VARCHAR(150) NOT NULL,
    InstitutionType VARCHAR(50) NOT NULL,
    PhoneNumber VARCHAR(100),
    Website VARCHAR(150)
);